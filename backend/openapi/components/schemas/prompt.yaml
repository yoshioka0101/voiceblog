type: object
properties:
  id:
    type: integer
    format: int64
  user_id:
    type: integer
    format: int64
    nullable: true
  name:
    type: string
  body:
    type: string
  is_active:
    type: boolean
  is_default:
    type: boolean
  created_at:
    type: string
    format: date-time
  updated_at:
    type: string
    format: date-time
required:
  - id
  - name
  - body
  - is_active
  - is_default
  - created_at
  - updated_at
